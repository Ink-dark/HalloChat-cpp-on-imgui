# HalloChat Monitor (Vite)

## Start
1. Install dependencies
   ```bash
   npm install
   ```
2. Run dev server
   ```bash
   npm run dev
   ```

## Usage
- 默认监控地址：`http://localhost:3001`
- 可在页面右上角输入不同服务端地址
